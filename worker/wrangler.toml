name = "golova-sync"
main = "src/worker.ts"
compatibility_date = "2024-09-01"

# Plain config. Override per-env via `wrangler deploy --var KEY:value`.
[vars]
OWNER = "saviorand"
REPO = "golova-kb"
BRANCH = "main"
ALLOWED_ORIGIN = "https://saviorand.github.io,http://localhost:8088"

# Secrets are set via:
#   wrangler secret put GITHUB_PAT
#   wrangler secret put BEARER_TOKEN
# Not stored here.
